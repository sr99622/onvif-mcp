<h2>ONVIF MCP server HTTP transport</h2>

start the server from the repository root like

```
uv run packages\http\src\main.py
```

The server will be listening on port 8001 with the endpoint /mcp i.e.

http://127.0.0.1:8001/mcp